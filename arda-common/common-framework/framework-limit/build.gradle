archivesBaseName = 'framework-limit'

dependencies {
    api(project(':arda-common:common-utils'))
    api(project(':arda-common:common-persistence:persistence-redis'))
    api('cn.keking.project:kk-anti-reptile:1.0.0-RELEASE')
}
jar.enabled = true
